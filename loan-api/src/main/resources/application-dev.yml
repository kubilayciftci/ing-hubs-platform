server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:loan-dev
    driver-class-name: org.h2.Driver
    username: inghubs-dev
    password: inghubsdev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
logging:
  level:
    root: info